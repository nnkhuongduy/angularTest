// import { routerReducer } from '@ngrx/router-store';
// import { ActionReducer, ActionReducerMap } from '@ngrx/store';
// import { ISlot } from '../models/search.model';
// import { searchSlotReducer, ISlotsState} from './slots/reducers/search-slot.reducer';


// export const reducers: ActionReducerMap<ISlotsState> = {
//     entities: searchSlotReducer,
//     router: routerReducer,
// };

// const debugMeta = (reducer: ActionReducer<any>): ActionReducer<any> => {
//     return (state, action) => {
//       console.log('state', state);
//       console.log('action', action);
  
//       return reducer(state, action);
//     };
//   };